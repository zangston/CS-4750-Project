function parseInput(input) {
    tokens = input.split(' ');
    response = ''

    if(tokens[0] == 'login')
    {   
        //TODO replace this with actual login code
        reponse = "login request recieved";
    }

    return reponse;
}